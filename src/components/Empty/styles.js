import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center'
  },

  cartText: {
    color: '#f3f9ff',
    fontSize: 28,
    marginTop: 28,
    lineHeight: 40,
    letterSpacing: 3,
  },

  emptyCart: {
    height: 160,
    width: 180,
  }
});
